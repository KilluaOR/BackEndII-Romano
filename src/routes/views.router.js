import { Router } from "express";

const router = Router();

// Aquí puedes agregar tus rutas de vistas más adelante
// Por ejemplo:
// router.get("/", (req, res) => {
//     res.render("home");
// });

export default router;
